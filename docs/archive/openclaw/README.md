# OpenClaw Prototype Archive

This folder preserves the earlier OpenClaw participant prototype. It is not part
of the supported Orchestra product surface and is not installed by the package.

Reintroduce this work only after Orchestra has first-class agent runtime
configuration, where OpenClaw can be selected as one supported agent type
alongside Codex or other runtimes.
